# Tell AI for Gemini CLI

Copy this directly to an AI agent:

```text
Install the Git Commit Message Skills repo for Gemini CLI by following docs/install-gemini-cli.md from https://github.com/zephyrcicd/ai-skill-git-commit-message. Clone the repo into ~/.gemini/git-commit-message, update ~/.gemini/GEMINI.md so Gemini knows where to load `skills/ggm/SKILL.md` and `skills/ggm-p/SKILL.md`, then verify the file references were added correctly.
```
