# Tell AI for Claude Code

Copy this directly to an AI agent:

```text
Install the Git Commit Message Skills repo for Claude Code by following docs/install-claude-code.md from https://github.com/zephyrcicd/ai-skill-git-commit-message. Use the documented marketplace flow. After installation, verify that the Claude Code plugin is installed and that the skills `ggm` and `ggm-p` are available.
```
